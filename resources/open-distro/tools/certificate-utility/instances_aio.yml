# Elasticsearch nodes
elasticsearch-nodes:
  - name: elasticsearch
    ip:
      - 127.0.0.1

# Wazuh server nodes
wazuh-servers:
  - name: filebeat
    ip:
      - 127.0.0.1      

# Kibana node
kibana:
  - name: kibana
    ip:
      - 127.0.0.1        